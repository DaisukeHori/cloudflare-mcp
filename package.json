{
  "name": "cloudflare-mcp",
  "version": "1.0.0",
  "description": "Comprehensive Cloudflare MCP Server — Tunnel, DNS, Zone, Registrar, Access, Billing, Workers, Pages, R2, KV, SSL",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "TRANSPORT=http tsx src/server.ts",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^4.21.2",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.3",
    "@vercel/node": "^5.7.5",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}
